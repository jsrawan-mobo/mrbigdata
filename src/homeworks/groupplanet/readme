******Folder Layout
doc - all usefull docs that we used, plus our own presentation
mrbigplanet - Our final project in eclipse format.
mrbigplanet/src - All project source should go here


******User folders: For temporary unfinished work... etc:

Jag 
Laura
Scott
Anil
Sandeep

****** Eclipse notes:

--- Libraries required
Python : Numpy, Scipy 
Graphviz : graphviz, python-graph, libgv-python, python-pygraphviz


OS : mrjob, boto-dist
Optional:

To add this mrbigplanet

1) Open up your existing workspace
2) Go to File->Import
3) Select General->Existing projects into workspace
4) Selet the mrbigplanet folder
Do NOT select []copy project sinto workspace!!
5) Click Finish
6) Run the TBD by right clicking and selecting (python run).



**** Required tools

1) Fix graphviz for python, the following doesn't work
import gz 

To Check which python libs you have

$ find /usr/lib/ -name "python"
$ ll -d /usr/share/pyshared/*/

link the libgv_python.so
$ cd /usr/lib/pyshared/python2.6 mv
$ sudo rm _gv.so
$ sudo ln -s libgv_python.so _gv.so

Add following to bashrc
PATH="/usr/lib/pyshared/python2.6:$PATH"


